# MCP工具使用指南

本文档说明ContentHub Skill使用的MCP工具及配置方法。

## MCP工具概览

```
网络搜索
├── fetch（默认，使用uvx）
└── tavily（可选，使用npx）

图片生成
└── doubao（豆包AI，使用npx）

文章发布
└── wenyan-mcp（微信公众号，使用npx）
```

---

## 1. 网络搜索工具

### 1.1 fetch（默认MCP服务）

**用途**：从信息源检索最新内容

**配置方式**：
```json
{
  "mcpServers": {
    "fetch": {
      "args": ["-y", "mcp-server-fetch"],
      "command": "uvx"
    }
  }
}
```

**使用场景**：
- 搜索最新美食资讯
- 查找餐厅信息
- 获取用户评价

---

### 1.2 tavily（可选MCP服务）

**用途**：高级网络搜索，提供更精准的搜索结果

**配置方式**：
```json
{
  "mcpServers": {
    "tavily-mcp": {
      "args": ["-y", "tavily-mcp@0.1.4"],
      "command": "npx",
      "disabled": false,
      "env": {
        "TAVILY_API_KEY": "your-api-key-here"
      }
    }
  }
}
```

**使用场景**：
- 需要更精准的搜索结果时
- 需要指定时间范围（如最近7天）
- 需要提取网页完整内容时

**注意事项**：
- 需要申请TAVILY_API_KEY
- 相比fetch功能更强大

---

## 2. 图片生成工具

### 2.1 doubao（豆包AI文生图）

**用途**：为文章生成配图

**配置方式**：
```json
{
  "mcpServers": {
    "doubao": {
      "args": ["-y", "@tonychaos/mcp-server-doubao"],
      "command": "npx",
      "disabledTools": [],
      "env": {
        "ARK_API_KEY": "your-api-key-here"
      }
    }
  }
}
```

**参数说明**：
- `model`: "doubao-seedream-4-5-251128"（推荐）
- `prompt`: 图片生成提示词（建议英文，50-150词）
- `size`: "2K"（推荐）或 "4K"（高清）
- `count`: 生成图片数量（1-15张）

**Prompt结构建议**：
```
[主体描述] + [环境/场景] + [风格/摄影手法] + [质量要求]

示例：
"Delicious bowl of ramen noodles with broth, eggs and green onions,
in a cozy Japanese restaurant setting, warm ambient lighting,
professional food photography style, 4K quality"
```

**返回值**：
- `images[0].url`: 生成的图片URL
- `images[0].size`: 图片尺寸（如"2848x1600"）

**重要提示**：
- ⚠️ URL有时效性（约24小时），生成后必须立即下载
- ⚠️ 禁止在文章中直接使用外部URL
- ✅ 下载到本地：`{账号}/output/{日期}/images/`
- ✅ 使用相对路径：`./images/{文件名}.jpg`

---

## 3. 文章发布工具

### 3.1 wenyan-mcp（微信公众号）

**用途**：发布文章到微信公众号

**配置方式**：
```json
{
  "mcpServers": {
    "wenyan-mcp": {
      "args": ["-y", "@wenyan-md/mcp"],
      "command": "npx",
      "env": {
        "WECHAT_APP_ID": "your-app-id-here",
        "WECHAT_APP_SECRET": "your-app-secret-here"
      }
    }
  }
}
```

#### Frontmatter 格式要求（必读）

每篇 Markdown 顶部需要包含 frontmatter：

```yaml
---
title: 文章标题
cover: 可选封面图路径
---
```

**字段说明**：
- `title`：必填，文章标题
- `cover`：可选
  - 如果正文有图片 → 可省略，自动使用第一张图片
  - 如果正文无图片 → 必须提供本地或网络图片路径

#### 正文格式要求

- 使用 Markdown 格式
- **重要**：有了 frontmatter 的 `title` 后，正文开头不要再写标题（避免重复）
- 图片支持：
  - 本地路径：`/Users/xxx/image.jpg`
  - 网络路径：`https://example.com/image.jpg`

#### 主题选择

可用主题（通过 `list_themes` 工具查看）：
- `default` - 经典布局，适合长文
- `orangeheart` - 温暖橙色调，活力风格（推荐快讯类）
- `rainbow` - 彩色活泼主题
- `lapis` - 清新蓝色调，简约风格
- `pie` - 现代、锐利、时尚
- `maize` - 柔和玉米色，清新明亮
- `purple` - 紫色调，极简主义
- `phycat` - 薄荷绿，结构清晰

#### 发布流程

1. 准备文章内容（带 frontmatter）
2. 调用 `publish_article` 工具
3. 选择合适的 `theme_id`
4. 发布到草稿箱
5. 登录微信公众平台后台检查
6. 确认无误后正式发布

#### 实践建议

- ✅ 使用 frontmatter 的 title，去掉正文开头的标题行
- ✅ 图片使用绝对路径更可靠
- ✅ 根据文章风格选择合适的主题
- ✅ 发布前先到草稿箱预览效果
- ⚠️ 多账号管理需要配置切换机制

**使用场景**：
- 自动发布文章到微信公众号
- 批量发布多篇文章
- 定时发布

**配置说明**：
- 需要在微信公众平台申请AppID和AppSecret
- 配置信息包含敏感信息，请妥善保管
- 建议在 `account-config.md` 中配置是否自动发布

---

## 4. 本地命令工具

### 4.1 目录操作

```bash
# 创建日期和图片目录
mkdir -p "{账号}/output/{日期}/images"

# 验证目录结构
ls -lh "{账号}/output/{日期}/"
```

### 4.2 图片下载

```bash
# 下载图片（推荐方式）
curl -L "{图片URL}" -o "{账号}/output/{日期}/images/{文件名}.jpg"

# 验证图片文件
file "{账号}/output/{日期}/images/{文件名}.jpg"
ls -lh "{账号}/output/{日期}/images/{文件名}.jpg"
```

### 4.3 创建图片清单

```bash
# 创建manifest.txt
cat > "{账号}/output/{日期}/images/manifest.txt" << 'EOF'
# {日期} 文章配图清单

## 文章: {文章名}

### 图片1: {图片描述}
- 文件名: {文件名}.jpg
- 来源: AI生成 / 网络下载（XX平台）
- 时间: {YYYY-MM-DD HH:MM}
- 用途: 文章头图 / 配图
EOF
```

---

## 5. MCP配置文件位置

MCP 配置文件通常位于以下位置之一：
- **Claude Code (CLI)**: `~/.claude.json` (推荐)
- **全局配置 (Claude Desktop)**: `~/.config/claude-code/mcp_config.json` (可选)

**推荐**：使用 `~/.claude.json`，因为它是账号管理脚本自动更新的目标。

---

## 5.1 MCP 自动安装流程

### 前置检查

安装前请确认系统环境：

```bash
# 检查 Node.js（必需）
node --version
npm --version

# 检查 Python/uvx（可选，用于 fetch）
python3 --version
uvx --version
```

### 自动安装脚本

创建一键安装脚本 `install-mcp-tools.sh`：

```bash
#!/bin/bash

echo "🚀 开始安装 MCP 工具..."

# 1. 检查 Node.js
if ! command -v node &> /dev/null; then
    echo "❌ 未检测到 Node.js，请先安装"
    exit 1
fi

# 2. 安装 wenyan-mcp（微信公众号）
echo "📦 安装 wenyan-mcp..."
npm install -g @wenyan-md/mcp

# 3. 安装 doubao-mcp（图片生成）
echo "🎨 安装 doubao-mcp..."
npm install -g @tonychaos/mcp-server-doubao

# 4. 安装 tavily-mcp（高级搜索，可选）
read -p "是否安装 tavily-mcp？(需要 API Key) [y/N] " -n 1 -r
echo
if [[ $REPLY =~ ^[Yy]$ ]]; then
    echo "🔍 安装 tavily-mcp..."
    npm install -g tavily-mcp@0.1.4
fi

# 5. 验证安装
echo "✅ 安装完成！正在验证..."

if command -v wenyan-mcp &> /dev/null; then
    echo "✅ wenyan-mcp 已安装"
else
    echo "⚠️  wenyan-mcp 可能需要使用 npx 运行"
fi

echo "📝 下一步：配置 MCP 服务"
echo "   1. 编辑 ~/.claude.json"
echo "   2. 添加 API Keys 到 .env 文件"
echo "   3. 重启 Claude Code"
```

**使用方法**：
```bash
# 保存脚本
chmod +x install-mcp-tools.sh

# 运行安装
./install-mcp-tools.sh
```

### 配置向导

安装完成后，运行配置向导：

```bash
#!/bin/bash

# config-mcp.sh - MCP 配置向导

echo "⚙️  MCP 配置向导"
echo "================"

echo "📂 配置文件位置: $HOME/.claude.json"

# 检测配置文件位置
# 默认指向 Claude Code CLI 的配置文件
CONFIG_FILE="$HOME/.claude.json"

# 备份现有配置
if [ -f "$CONFIG_FILE" ]; then
    cp "$CONFIG_FILE" "$CONFIG_FILE.backup"
    echo "✅ 已备份现有配置"
fi

# 读取环境变量
echo ""
echo "请输入 API 配置："
read -p "ARK_API_KEY (豆包AI): " ARK_KEY
read -p "TAVILY_API_KEY (可选): " TAVILY_KEY
read -p "WECHAT_APP_ID (公众号): " WECHAT_ID
read -sp "WECHAT_APP_SECRET (公众号): " WECHAT_SECRET
echo ""

# 生成配置
cat > "$CONFIG_FILE" << EOF
{
  "mcpServers": {
    "doubao": {
      "command": "npx",
      "args": ["-y", "@tonychaos/mcp-server-doubao"],
      "env": {
        "ARK_API_KEY": "$ARK_KEY"
      }
    },
    "wenyan-mcp": {
      "command": "npx",
      "args": ["-y", "@wenyan-md/mcp"],
      "env": {
        "WECHAT_APP_ID": "$WECHAT_ID",
        "WECHAT_APP_SECRET": "$WECHAT_SECRET"
      }
    }
  }
}
EOF

echo "✅ 配置已保存到: $CONFIG_FILE"
echo "🔄 请重启 Claude Code 使配置生效"
```

### 验证安装

重启 Claude Code 后，运行以下命令验证：

```bash
# 检查 MCP 服务是否加载
# 在 Claude Code 中执行：
# - 使用 list_themes 工具测试 wenyan-mcp
# - 使用 generate_image 工具测试 doubao
# - 使用 tavily-search 工具测试 tavily
```

### 故障排除

**问题1**: npx 找不到命令
```bash
# 解决方案：重新安装 Node.js 并确保添加到 PATH
```

**问题2**: MCP 服务未加载
```bash
# 检查配置文件语法
cat ~/.claude.json | python3 -m json.tool

# 查看日志
tail -f ~/Library/Logs/Claude/claude-code.log
```

**问题3**: 权限错误
```bash
# macOS/Linux
chmod +x ~/.claude.json
```

---

## 6. 常见问题处理

### Q1: fetch无法使用怎么办？

**A**: 按优先级使用备用工具：
1. 检查uvx是否安装：`uvx --version`
2. 安装uvx：`pip install uvx`
3. 或使用tavily作为替代

### Q2: 图片生成失败怎么办？

**A**:
1. 检查ARK_API_KEY是否正确
2. 优化Prompt（增加具体描述细节）
3. 简化Prompt（去掉过于复杂的描述）
4. 重新生成（最多3次）

### Q3: 图片下载失败怎么办？

**A**:
1. 确认URL完整（包含所有参数）
2. 使用 `curl -L`（跟随重定向）
3. 检查网络连接
4. 尝试使用wget替代curl

### Q4: 如何确保图片与文章相关？

**A**:
1. 生成图片前进行内容分析
2. 编写Prompt时包含文章关键词
3. 指定图片风格（如"美食摄影"、"商务场景"）
4. 生成后人工验证

### Q5: 微信公众号发布失败？

**A**:
1. 检查AppID和AppSecret是否正确
2. 确认账号有发布权限
3. 检查文章格式是否符合要求
4. 查看错误日志获取详细原因

---

## 7. 最佳实践

### 网络搜索
- 优先使用时间范围参数（如"最近7天"）
- 使用具体的关键词提高搜索精度
- 多源验证，交叉对比信息

### 图片生成
- 使用详细的Prompt（包含主体、环境、风格）
- 生成后立即下载（URL只有24小时有效期）
- 使用manifest.txt记录所有图片信息
- 图片命名规范：`YYYY-MM-DD-{板块}-{序号}.jpg`

### 文章发布
- 发布前进行质量检查
- 先发布到测试平台验证
- 设置发布前检查（图片、格式、链接等）
- 记录发布结果便于追溯

---

## 8. MCP调用约束

**重要**：在工作流文档中，涉及MCP的步骤只写"需要系统完成的动作/要求"，不写任何调用代码。

**正确示例**：
```
要求系统完成：
- 使用fetch对每个监控站点检索当日新闻
- 调用豆包AI生成配图
- 下载图片到本地并创建manifest.txt
```

**错误示例**：
```
mcp__tavily-mcp__tavily-search(query="...")
```

---

## 9. 相关文档

- [SKILL.md](../SKILL.md) - 主技能文档
- [quality-gates.md](./quality-gates.md) - 质量门禁规范
- [USER-GUIDE.md](../USER-GUIDE.md) - 用户使用指南
