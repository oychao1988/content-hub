# ContentHub CLI 变化总结报告

**报告日期**: 2026-02-08
**版本**: 2.0.0
**变化范围**: 异步内容生成系统引入的 CLI 变化

---

## 📊 变化概览

| 变化类型 | 数量 | 说明 |
|---------|------|------|
| **新增命令组** | 2 个 | task, monitor |
| **修改命令** | 1 个 | content generate（新增参数） |
| **新增命令** | 12 个 | task: 6个，monitor: 6个 |
| **新增参数** | 2 个 | --async, --auto-approve |

---

## 1️⃣ 新增命令组

### 1.1 task 命令组（异步任务管理）

**命令组说明**: 管理异步内容生成任务

| 命令 | 功能 | 参数 | 示例 |
|------|------|------|------|
| `task status <task_id>` | 查询任务状态 | task_id | `contenthub task status task-abc123` |
| `task list` | 列出任务 | --status, --limit, --account-id | `contenthub task list --status pending` |
| `task cancel <task_id>` | 取消任务 | task_id | `contenthub task cancel task-abc123` |
| `task retry <task_id>` | 重试失败任务 | task_id | `contenthub task retry task-abc123` |
| `task cleanup` | 清理旧任务 | --days, --yes | `contenthub task cleanup --days 30` |
| `task stats` | 显示任务统计 | 无 | `contenthub task stats` |

**详细说明**:

#### task status
```bash
# 查询任务状态
contenthub task status task-abc123def456

# 输出示例：
⏳ 任务信息
   任务ID: task-abc123def456
   状态: processing
   进度: 75%
   开始时间: 2026-02-08 08:00:05
   预计完成: 2026-02-08 08:04:05
```

#### task list
```bash
# 列出所有待处理任务
contenthub task list --status pending --limit 10

# 列出特定账号的任务
contenthub task list --account-id 49

# 输出格式：table（默认）| json | csv
contenthub task list --format json
```

#### task cancel
```bash
# 取消任务
contenthub task cancel task-abc123def456

# 输出示例：
✅ 任务已取消
   任务ID: task-abc123def456
   原状态: pending
   新状态: cancelled
```

#### task retry
```bash
# 重试失败任务
contenthub task retry task-abc123def456

# 输出示例：
✅ 任务已重试
   原任务ID: task-abc123def456
   新任务ID: task-def456ghi789
   重试次数: 1/3
```

#### task cleanup
```bash
# 清理30天前的已完成任务
contenthub task cleanup --days 30

# 强制清理（不确认）
contenthub task cleanup --days 30 --yes

# 输出示例：
✅ 清理完成
   删除任务数: 150
   释放空间: 2.3 MB
```

#### task stats
```bash
# 显示任务统计
contenthub task stats

# 输出示例：
📊 任务统计信息
   总任务数: 1,234
   今日任务: 45
   状态分布：
     - pending: 12
     - submitted: 8
     - processing: 5
     - completed: 1,150
     - failed: 38
     - cancelled: 21
```

---

### 1.2 monitor 命令组（异步任务监控）

**命令组说明**: 监控异步任务系统状态和性能

| 命令 | 功能 | 参数 | 示例 |
|------|------|------|------|
| `monitor metrics` | 显示综合指标 | 无 | `contenthub monitor metrics` |
| `monitor recent` | 显示最近任务 | --limit | `contenthub monitor recent --limit 10` |
| `monitor failed` | 显示失败任务 | --limit | `contenthub monitor failed --limit 20` |
| `monitor pending` | 显示待处理任务 | --limit | `contenthub monitor pending --limit 10` |
| `monitor stats` | 显示每日统计 | --days | `contenthub monitor stats --days 7` |
| `monitor health` | 显示系统健康状态 | 无 | `contenthub monitor health` |

**详细说明**:

#### monitor metrics
```bash
# 显示综合指标
contenthub monitor metrics

# 输出示例：
📊 异步任务监控指标
   总任务数: 1,234
   今日任务: 45
   运行中: 5
   待处理: 12
   成功率: 96.8%
   平均耗时: 3.2 分钟
   系统状态: ✅ 健康
```

#### monitor recent
```bash
# 显示最近10个任务
contenthub monitor recent --limit 10

# 输出示例：
📋 最近的任务
   ┌──────┬──────────────┬─────────┬──────────┬────────┐
   │ ID   │ 账号         │ 选题    │ 状态     │ 耗时   │
   ├──────┼──────────────┼─────────┼──────────┼────────┤
   │ 123  │ 微信公众号A  │ AI技术  │ completed│ 3:15   │
   │ 124  │ 微信公众号B  │ 机器学习│ processing│ -      │
   └──────┴──────────────┴─────────┴──────────┴────────┘
```

#### monitor failed
```bash
# 显示最近20个失败任务
contenthub monitor failed --limit 20

# 输出示例：
❌ 失败的任务
   ┌──────┬──────────────┬──────────┬────────────────┐
   │ ID   │ 账号         │ 失败时间 │ 错误信息       │
   ├──────┼──────────────┼──────────┼────────────────┤
   │ 125  │ 微信公众号A  │ 08:15:23 │ 超时           │
   │ 126  │ 微信公众号C  │ 08:12:10 │ API错误        │
   └──────┴──────────────┴──────────┴────────────────┘
```

#### monitor pending
```bash
# 显示待处理任务
contenthub monitor pending --limit 10

# 输出示例：
⏳ 待处理任务
   总数: 12
   优先级高: 3
   优先赛中: 5
   优先级低: 4
```

#### monitor stats
```bash
# 显示最近7天统计
contenthub monitor stats --days 7

# 输出示例：
📊 每日统计（最近7天）
   ┌────────────┬────────┬────────┬────────┬──────────┐
   │ 日期       │ 总数   │ 成功   │ 失败   │ 成功率   │
   ├────────────┼────────┼────────┼────────┼──────────┤
   │ 2026-02-08 │ 45     │ 43     │ 2      │ 95.6%    │
   │ 2026-02-07 │ 52     │ 50     │ 2      │ 96.2%    │
   │ 2026-02-06 │ 38     │ 38     │ 0      │ 100.0%   │
   └────────────┴────────┴────────┴────────┴──────────┘
```

#### monitor health
```bash
# 显示系统健康状态
contenthub monitor health

# 输出示例：
🏥 系统健康检查
   数据库: ✅ 正常
   队列服务: ✅ 正常
   Worker池: ✅ 正常（5/5运行中）
   轮询器: ✅ 运行中
   系统状态: ✅ 健康
```

---

## 2️⃣ 修改的命令

### 2.1 content generate

**原命令**:
```bash
contenthub content generate \
  --account-id 1 \
  --topic "AI 技术"
```

**新增参数**:

| 参数 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| `--async` | flag | 异步模式 | 关闭 |
| `--auto-approve` / `--no-auto-approve` | flag | 自动审核 | auto-approve |

**新用法示例**:

#### 异步模式
```bash
# 异步生成（立即返回）
contenthub content generate \
  --account-id 49 \
  --topic "人工智能发展趋势" \
  --keywords "AI,机器学习" \
  --async

# 输出示例：
✅ 异步任务已提交
   任务ID: task-abc123def456
   状态: pending
   提交时间: 2026-02-08 23:20:15
```

#### 同步模式（默认）
```bash
# 同步生成（阻塞等待）
contenthub content generate \
  --account-id 49 \
  --topic "人工智能发展趋势"

# 输出示例：
⏳ 正在生成内容，请稍候...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 100%

✅ 内容生成成功
   内容ID：123
   标题：人工智能发展趋势
   字数：1,526
```

#### 自动审核控制
```bash
# 异步生成 + 自动审核（默认）
contenthub content generate \
  --account-id 49 \
  --topic "AI技术" \
  --async \
  --auto-approve

# 异步生成 + 手动审核
contenthub content generate \
  --account-id 49 \
  --topic "AI技术" \
  --async \
  --no-auto-approve
```

---

## 3️⃣ 向后兼容性

### ✅ 完全兼容

所有原有命令保持不变，异步参数为可选：

| 场景 | 兼容性 | 说明 |
|------|--------|------|
| 不使用 `--async` | ✅ 完全兼容 | 默认同步模式 |
| 原有脚本 | ✅ 无需修改 | 所有参数保留 |
| 输出格式 | ✅ 保持一致 | 使用相同的输出样式 |

---

## 4️⃣ 文档更新需求

### ❌ 需要更新的文档

| 文档 | 更新内容 | 优先级 |
|------|---------|--------|
| `docs/references/CLI-REFERENCE.md` | 添加 task 和 monitor 命令组 | 🔴 高 |
| `docs/references/CLI-REFERENCE.md` | 更新 content generate 命令说明 | 🔴 高 |
| `docs/references/CLI-REFERENCE.md` | 更新目录结构 | 🟡 中 |
| `docs/guides/cli-quick-start.md` | 添加异步使用示例 | 🟡 中 |

### ✅ 已存在的文档

| 文档 | 状态 | 说明 |
|------|------|------|
| `docs/guides/async-content-cli-quick-reference.md` | ✅ 已创建 | 异步 CLI 快速参考 |
| `docs/guides/async-content-user-guide.md` | ✅ 已创建 | 完整用户指南 |

---

## 5️⃣ 更新建议

### 5.1 主要更新内容

#### CLI-REFERENCE.md 需要添加：

1. **目录新增**
```markdown
- [任务管理 (task)](#任务管理-task)
- [监控管理 (monitor)](#监控管理-monitor)
```

2. **新增 task 命令组章节**
```markdown
## 任务管理 (task)

### 命令列表
| 命令 | 说明 |
|------|------|
| task status | 查询任务状态 |
| task list | 列出任务 |
| ... | ... |

### 详细说明
#### task status
...
```

3. **新增 monitor 命令组章节**
```markdown
## 监控管理 (monitor)

### 命令列表
| 命令 | 说明 |
|------|------|
| monitor metrics | 显示综合指标 |
| monitor recent | 显示最近任务 |
| ... | ... |

### 详细说明
#### monitor metrics
...
```

4. **更新 content generate 命令**
```markdown
#### content generate

**新增参数**:
- `--async`: 异步模式（立即返回）
- `--auto-approve/--no-auto-approve`: 自动审核开关

**异步模式示例**:
\`\`\`bash
contenthub content generate \
  --account-id 49 \
  --topic "AI技术" \
  --async
\`\`\`
```

### 5.2 更新步骤

1. 更新 `docs/references/CLI-REFERENCE.md`
   - 添加 task 命令组（约300行）
   - 添加 monitor 命令组（约300行）
   - 更新 content generate 命令（约50行）

2. 更新 `docs/guides/cli-quick-start.md`
   - 添加异步使用示例（约100行）

3. 验证文档完整性
   - 检查所有命令示例
   - 确保参数说明准确
   - 验证输出示例格式

---

## 6️⃣ 快速参考卡片

### 异步工作流

```bash
# 1. 提交异步任务
contenthub content generate -a 49 -t "AI技术" --async

# 2. 查询任务状态
contenthub task status task-abc123

# 3. 监控系统
contenthub monitor metrics

# 4. 查看失败任务
contenthub monitor failed

# 5. 重试失败任务
contenthub task retry task-abc123
```

### 定时任务集成

```bash
# 创建每日异步生成任务
scheduler create \
  --name "每日内容生成" \
  --type async_content_generation \
  --cron "0 8 * * *" \
  --params '{"account_ids": [49], "count_per_account": 3}'
```

---

## 7️⃣ 总结

### 新增功能

- ✅ 2个新命令组（task, monitor）
- ✅ 12个新命令
- ✅ 2个新参数（--async, --auto-approve）

### 文档状态

- ✅ 异步专用文档已创建
- ❌ 主 CLI 参考文档待更新
- ❌ CLI 快速入门待更新

### 建议

**立即更新**: `docs/references/CLI-REFERENCE.md`
**次要更新**: `docs/guides/cli-quick-start.md`
**可选更新**: 创建异步命令速查表

---

**报告人**: Claude Code
**报告日期**: 2026-02-08
**版本**: 2.0.0
